{
  "name": "@basilico/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "basilico from a terminal: the same core, driving the same cycle, for coding agents.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@basilico/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
